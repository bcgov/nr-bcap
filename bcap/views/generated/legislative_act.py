"""Read-only Legislative Act API, scoped to the requesting user's own resources:
a list of the user's Legislative Act resources and a by-id detail fetch.

This module was generated by the ``generate_graph_views`` management command.
Do not edit by hand; re-run the command instead.
"""

from drf_spectacular.utils import extend_schema
from rest_framework.generics import ListAPIView, RetrieveAPIView
from rest_framework.permissions import IsAuthenticated

from arches_querysets.rest_framework.pagination import ArchesLimitOffsetPagination
from arches_querysets.rest_framework.view_mixins import ArchesModelAPIMixin

from arches_zod_validation.views.mixins import (
    ArchesResourceViewMixin,
    BCAPResourceSerializer,
    UserOwnedResourceMixin,
)


class LegislativeActSerializer(BCAPResourceSerializer):
    class Meta(BCAPResourceSerializer.Meta):
        graph_slug = "legislative_act"


class LegislativeActViewMixin(ArchesResourceViewMixin):
    serializer_class = LegislativeActSerializer


@extend_schema(tags=["External: legislative_act"])
class LegislativeActListView(
    LegislativeActViewMixin,
    UserOwnedResourceMixin,
    ArchesModelAPIMixin,
    ListAPIView,
):
    """GET the requesting user's Legislative Act resources.

    Read-only and owner-scoped: returns only the resources the user created.
    """

    permission_classes = [IsAuthenticated]
    pagination_class = ArchesLimitOffsetPagination


@extend_schema(tags=["External: legislative_act"])
class LegislativeActView(
    LegislativeActViewMixin,
    UserOwnedResourceMixin,
    ArchesModelAPIMixin,
    RetrieveAPIView,
):
    """GET a Legislative Act resource and its nested tiles.

    Read-only, and scoped to the requesting user's own resources: requesting
    one created by another user returns 404.
    """

    permission_classes = [IsAuthenticated]
