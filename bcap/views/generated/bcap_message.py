"""BCAP Message API, scoped to the requesting user's own resources: a
collection endpoint and a by-id detail endpoint. Verbs (POST)
come from generated/generate.json -- edit that file and regenerate to change
which methods each endpoint exposes.

This module was generated by the ``generate_graph_views`` management command.
Do not edit by hand; re-run the command instead.
"""

from drf_spectacular.utils import extend_schema
from rest_framework.generics import CreateAPIView, RetrieveAPIView
from rest_framework.permissions import IsAuthenticated

from arches_querysets.rest_framework.pagination import ArchesLimitOffsetPagination
from arches_querysets.rest_framework.view_mixins import ArchesModelAPIMixin

from arches_zod_validation.views.mixins import (
    ArchesResourceViewMixin,
    BCAPResourceSerializer,
    UserOwnedResourceMixin,
)


class BcapMessageSerializer(BCAPResourceSerializer):
    class Meta(BCAPResourceSerializer.Meta):
        graph_slug = "bcap_message"


class BcapMessageViewMixin(ArchesResourceViewMixin):
    serializer_class = BcapMessageSerializer


@extend_schema(tags=["External: bcap_message"])
class BcapMessageListView(
    BcapMessageViewMixin,
    UserOwnedResourceMixin,
    ArchesModelAPIMixin,
    CreateAPIView,
):
    """Collection endpoint for the requesting user's BCAP Message resources.

    Owner-scoped: only the resources the user created. Declared verbs:
    POST (from generated/generate.json); the CreateAPIView base
    class serves the collection subset (GET/POST), narrowed to the declared
    verbs by http_method_names.
    """

    http_method_names = ["post", "head", "options"]
    permission_classes = [IsAuthenticated]
    pagination_class = ArchesLimitOffsetPagination


@extend_schema(tags=["External: bcap_message"])
class BcapMessageView(
    BcapMessageViewMixin,
    UserOwnedResourceMixin,
    ArchesModelAPIMixin,
    RetrieveAPIView,
):
    """Detail endpoint for a single BCAP Message resource and its nested tiles.

    Owner-scoped: requesting one created by another user returns 404. Declared
    verbs: POST (from generated/generate.json); the
    RetrieveAPIView base class serves the detail subset
    (GET/PUT/PATCH/DELETE), narrowed to the declared verbs by http_method_names.
    """

    http_method_names = ["head", "options"]
    permission_classes = [IsAuthenticated]
